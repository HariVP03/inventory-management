import React from "react";
import { Button, Flex } from "@chakra-ui/react";

const Home: React.FC = () => {
    return (
        <Flex
            gap="10px"
            w="100vw"
            justify="center"
            h="50px"
            align="center"
        ></Flex>
    );
};

export default Home;
